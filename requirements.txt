# Core dependencies
fastapi>=0.109.0
starlette>=0.31.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
asyncpg>=0.29.0
sqlalchemy>=2.0.25
alembic>=1.13.0
pgvector>=0.2.4
greenlet>=3.0.0
aiosqlite>=0.19.0
# Required for psycopg2-based scripts (e.g., tenant data loader)
psycopg2-binary>=2.9.0

# Async HTTP client
httpx>=0.26.0

# LLM integration
google-genai
llama-cpp-python>=0.1.0

# Kafka
aiokafka>=0.10.0

# Parquet / Telemetry replay
pyarrow>=14.0.0

# Utilities
python-dotenv>=1.0.0
structlog>=24.1.0
json5>=0.13.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.7

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.26.0
aiosqlite>=0.19.0
locust>=2.20.0

# Datasets
datasets>=2.16.0
# Upper-bounded — transformers 4.44.2 expects hf-hub <1.0; a future 1.x
# major could break the same trust_remote_code path we just fixed.
huggingface_hub>=0.23.2,<1.0

# Scientific / Causal AI
numpy>=1.26.0
pandas>=2.0.0
statsmodels>=0.14.0
# T-VEC (NetoAISolutions/T-VEC) ships trust_remote_code modeling files written
# against transformers 4.x. transformers 5.x changes Qwen2Config (rope_theta
# moved) and breaks the remote code with an AttributeError at load time.
# Hard-pin the ML core; loosen only after testing a specific newer combo end-to-end.
sentence-transformers==3.0.1
transformers==4.44.2
# transformers 4.44.2 pins tokenizers<0.20, but T-VEC's tokenizer.json was
# saved with tokenizers>=0.20 (new ModelWrapper variants). Override the pin;
# the Python API is backward-compatible.
tokenizers>=0.20,<0.22
torch>=2.2.0,<3.0
tigramite>=0.9.0

# Observability
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-instrumentation-fastapi>=0.43b0
 
# Netconf client for device configuration (P5.4)
ncclient>=0.6.14
