# Cloud deployment dependencies
# Excludes: locust (load testing, dev-only)
# Adds: pyarrow (Parquet loading for Telco2 dataset)

# Core dependencies
fastapi>=0.109.0
starlette>=0.31.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
asyncpg>=0.29.0
sqlalchemy>=2.0.25
alembic>=1.13.0
pgvector>=0.2.4
greenlet>=3.0.0
aiosqlite>=0.19.0
psycopg2-binary>=2.9.0

# Async HTTP client
httpx>=0.26.0

# LLM integration
google-genai
# llama-cpp-python intentionally excluded from cloud build — TSLAM inference
# is handled by the Ollama container via HTTP. The llama_cpp path in
# tslam_service.py is only triggered when TSLAM_GGUF_PATH exists inside the
# backend container, which it never does on cloud. Compiling from source
# takes ~10 min on ARM and produces dead code for this deployment.

# Kafka
aiokafka>=0.10.0

# Utilities
python-dotenv>=1.0.0
structlog>=24.1.0
json5>=0.13.0
simpleeval>=1.0.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.7

# Datasets
datasets>=2.16.0
# Upper-bounded — transformers 4.44.2 expects hf-hub <1.0; a future 1.x
# major could break the same trust_remote_code path we just fixed.
huggingface_hub>=0.23.2,<1.0

# Scientific / Causal AI
numpy>=1.26.0
pandas>=2.0.0
statsmodels>=0.14.0
# T-VEC (NetoAISolutions/T-VEC) ships trust_remote_code modeling files written
# against transformers 4.x. transformers 5.x changes Qwen2Config (rope_theta
# moved) and breaks the remote code with an AttributeError at load time.
# Hard-pin the ML core; loosen only after testing a specific newer combo end-to-end.
sentence-transformers==3.0.1
transformers==4.44.2
torch>=2.2.0,<3.0
tigramite>=0.9.0

# Observability
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-instrumentation-fastapi>=0.43b0

# Netconf client for device configuration
ncclient>=0.6.14

# Parquet loading (Telco2 dataset)
pyarrow>=14.0.0
